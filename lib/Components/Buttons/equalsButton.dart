import 'package:flutter/material.dart';

class CustomEqualsButtom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialButton(
      onPressed: () {},
    );
  }
}
